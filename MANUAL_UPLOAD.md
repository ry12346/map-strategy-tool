# GitHub Pagesへ手動で公開する手順

1. このZIPを展開します。
2. 展開したフォルダの**中身をすべて**GitHubリポジトリ直下へアップロードします。
3. `index.html`、`app.js`、`route_worker.js`、`style.css`、`data` フォルダが同じ構成で存在することを確認します。
4. GitHubのリポジトリで **Settings → Pages** を開きます。
5. 公開元に既定ブランチのルートを指定して保存します。
6. 発行されたURLを開き、PK1標準マップが表示されることを確認します。

`data` フォルダ内のファイルは経路探索に必要なので、省略せずアップロードしてください。

更新版へ差し替える場合は、ZIP内のファイルを同じ場所へ上書きします。利用者が保存した `.nssmap` は利用者側のファイルなので、Web側の更新では消えません。
