# 手動アップロード手順

ChatGPTとGitHubアカウントの連携は必要ありません。ブラウザからファイルを手動でアップロードして公開します。

## GitHub Pagesへ手動で公開する場合

1. GitHubで公開用リポジトリを作成します。
2. このフォルダ内のファイルを、フォルダ構成を変えずにリポジトリ直下へアップロードします。
3. 少なくとも次のファイルが直下にあることを確認します。

```text
index.html
app.js
style.css
map-maker.html
map-maker.js
map-maker.css
```

4. リポジトリのPages設定で、公開元として既定ブランチのルートを指定します。
5. 発行されたURLを開き、作戦図エディタが表示されることを確認します。

## 更新方法

修正版のZIPを受け取った場合は、変更されたファイルを同じ場所へ上書きアップロードします。作戦データは `.nssmap` ファイルとして利用者側に保存されるため、Web側のファイルを更新しても、保存済み作戦ファイルは消えません。

## 一門内だけで使う場合

公開URLを検索エンジンや外部掲示板へ掲載せず、一門内のDiscordなどで共有します。本ツールには検索エンジン向けの `noindex` 設定を入れています。ただし、これはアクセス制限ではありません。通常の静的Web公開だけでは、URLを知っている人の閲覧を完全には制限できません。

作戦内容そのものをWebサーバーへ保存する機能はありません。共有用HTMLまたはPNGを、必要な相手にだけ送付してください。

## アップロード前の確認

- `index.html` と関連ファイルを同じ階層関係でアップロードしている
- `map-maker.html` を単独で移動していない
- ファイル名の大文字・小文字を変更していない
- ZIPファイルのままではなく、展開した中身をアップロードしている
